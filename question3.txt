If we remove #include <stdio.h> and still use printf, the compiler won’t know what printf is.
You’ll see an error like “undefined reference to printf.”
The problem happens during linking, because it can’t find the function in the library.
